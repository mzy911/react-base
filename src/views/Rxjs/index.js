import React from 'react'
import * as rxjs from 'rxjs'





const Rxjs = () => {

  console.log('rxjs', rxjs);

  // var subscription = rxjs.interval(500).take(4).subscribe(function (x) { console.log(x) });
  
  return <div>
    Rxjs
  </div>
}

export default Rxjs;